name: PFP Bootstrap Chunk Integrity — Superseded

on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded transport notice
        run: |
          echo 'This binary chunk transport is no longer an admissible replay source.'
          echo 'The canonical source is the applied SQL retained in D3 supabase_migrations.schema_migrations.'
          echo 'See supabase/history/D3_CANONICAL_MIGRATION_LEDGER_20260915.md.'
          exit 1
