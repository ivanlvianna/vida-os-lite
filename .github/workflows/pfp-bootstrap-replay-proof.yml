name: PFP Bootstrap Replay Proof — Superseded Binary Transport

on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded proof notice
        run: |
          echo 'This workflow is intentionally retired.'
          echo 'The connector-transported gzip/tar artifacts failed byte-integrity checks.'
          echo 'Canonical replay source is now supabase_migrations.schema_migrations in D3.'
          echo 'See supabase/history/D3_CANONICAL_MIGRATION_LEDGER_20260915.md.'
          exit 1
