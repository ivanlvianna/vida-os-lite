name: PFP D3 Strict Replay Gate — Transport Blocked

on:
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Report strict replay gate status
        run: |
          echo 'PC-INDEPENDENT-BOOTSTRAP-REPLAY-GATE-001=OPEN'
          echo 'Reason: exact D3 ledger SQL is canonical, but no safe direct Supabase-to-GitHub file transport is available through current connectors.'
          echo 'Do not use superseded binary archives or oversized manual Base64 chunks.'
          echo 'Qualified Gate 0 standalone bootstrap remains valid and separate from this strict 30-migration replay gate.'
